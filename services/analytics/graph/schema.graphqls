type Mutation {
  createSnapshot(characterID: ObjectID!, description: String): Snapshot!
}

type Query {
  snapshots(characterID: ObjectID, filter: SnapshotFilter): [Snapshot!]!
}

input SnapshotFilter {
  month: Month
  year: Int
}
