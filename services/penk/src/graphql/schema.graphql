type Query {
  userContext: UserContext!
  messages: [Message!]!
}

type UserContext {
  id: ID!
  profileID: ID!
  timezone: String!
  locale: String!
  preferences: Preferences!
  context: String!
}

type Preferences {
  tone: String!
}

type Message {
  id: ID!
  userID: ID!
  type: MessageType!
  content: String!
  timestamp: String!
}

enum MessageType {
  USER_MESSAGE
  AI_MESSAGE
  AI_ERROR
}

